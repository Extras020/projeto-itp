#include <stdio.h>
#include <string.h>

int main(){

    char nome[10];
    char frequencia[1000];
    fgets(nome, 10, stdin);
    fgets(frequencia, 1000, stdin);
    int i, cont = 0;
    for(i = 0; i < 1000; i++){

    }
    return 0;
}
